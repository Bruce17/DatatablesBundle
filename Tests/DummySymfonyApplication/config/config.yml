framework:
    secret: test
    session:
        storage_id: session.storage.mock_file

    default_locale: en

twig:
    form_themes:
        # Default:
        - form_div_layout.html.twig

        # Bootstrap:
        - bootstrap_3_layout.html.twig
        - bootstrap_3_horizontal_layout.html.twig

        # Foundation
        - foundation_5_layout.html.twig

#    paths:
#        '%kernel.root_dir%/../../..': ~
#        '%kernel.root_dir%/../FunctionalTests/views': test
